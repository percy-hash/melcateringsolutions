# TODO

## Fix page starting/scroll jumping to last section/slide
- [ ] Inspect current slider logic (hash/:target) and autoplay behavior
- [ ] Update `Index.html` slider markup/scripts to remove `location.hash` usage during autoplay
- [ ] Update `style.css` to use class-based slider position instead of `:target`
- [ ] Ensure default visible slide is Slide 1 on page load
- [ ] Verify no jump to last section occurs when loading the page

